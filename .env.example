# Server-only. Never expose this in frontend JavaScript.
SCORE_SIGNING_SECRET=replace-with-32-plus-random-bytes

# Public AdSense publisher id. This is not a secret, but keep it configurable.
# Example: ca-pub-1234567890123456
PUBLIC_ADSENSE_CLIENT_ID=

# Optional deployment metadata.
NODE_ENV=production
